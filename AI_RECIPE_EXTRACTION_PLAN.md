# AI 레시피 추출 기능 개발 계획

## 목적

사용자가 유튜브 쇼츠 요리 영상 링크를 등록하면 앱이 다음 정보를 초안으로 생성한다.

- 요리 제목
- 필요 재료
- 재료별 추정 용량
- 조리 순서
- 영상에서 확인되지 않은 추정 정보
- 사용자가 검토하고 수정할 수 있는 저장용 레시피 데이터

이 기능의 1차 목표는 완전 자동 저장이 아니라, 사용자가 빠르게 수정할 수 있는 레시피 초안을 만드는 것이다.

## 기본 방향

처음부터 AI 모델을 직접 학습하지 않는다. MVP 단계에서는 오픈소스 모델과 도구를 조합해 레시피 추출 파이프라인을 만들고, 실제 사용자 수정 데이터를 모은 뒤 자체 모델 개발 또는 파인튜닝을 검토한다.

초기 개발 방향은 다음과 같다.

- 영상 링크 처리와 레시피 추출 흐름을 먼저 구현한다.
- 지원 플랫폼은 유튜브 쇼츠로 한정한다.
- 영상 제목과 설명란을 요리 제목, 재료, 조리 순서 추출의 1차 데이터로 사용한다.
- 음성 자막 기반 추출을 1차 범위로 둔다.
- 화면 텍스트 OCR은 재료 누락을 줄이는 보조 수단으로 초기에 검토한다.
- 고비용 영상 프레임 분석과 VLM 추론은 OCR 이후의 2차 범위로 둔다.
- 추출 결과는 항상 사용자가 수정할 수 있게 제공한다.
- AI가 추정한 정보와 영상에서 명확히 확인된 정보를 구분한다.

## 전체 처리 흐름

```text
사용자 링크 입력
→ 링크 유효성 검증
→ 영상 제목, 설명란, 길이 등 메타데이터 확인
→ 요리 영상 여부 1차 판별
→ 오디오 추출
→ 필요 시 보컬 분리
→ STT로 음성 텍스트 변환
→ 필요 시 프레임 OCR 수행
→ 필요 시 VLM으로 영상 프레임 분석
→ 메타데이터, STT, OCR, VLM 결과를 LLM에 전달
→ 구조화된 JSON 생성
→ 사용자 검토 및 수정
→ 레시피로 저장
```

## MVP 범위

MVP에서는 다음 기능만 우선 구현한다.

- 유튜브 쇼츠 URL 입력
- 영상 길이 제한
- 영상 제목과 설명란 기반 요리 정보 추출
- 오디오 기반 텍스트 추출
- 영상 설명란과 STT 결과 기반 추출
- 텍스트 기반 요리 제목, 재료, 조리 순서 추출
- 요리 영상이 아닌 링크를 걸러내는 1차 판별
- 추출 결과를 레시피 작성 화면에 초안으로 채우기
- 사용자가 수정 후 저장

MVP에서 제외하는 항목은 다음과 같다.

- 유튜브 쇼츠가 아닌 외부 영상 플랫폼 처리
- 일반 유튜브 긴 영상 처리
- VLM 기반 영상 프레임 재료 인식
- 자체 AI 모델 학습
- 동영상 파일 직접 업로드
- 완전 자동 레시피 저장
- 공개 레시피로 즉시 등록

MVP에서 선택적으로 검토할 항목은 다음과 같다.

- 프레임 OCR 기반 화면 텍스트 추출
- 배경음악이 큰 영상의 보컬 분리 전처리

## 기술 후보

### 링크 및 영상 처리

- `yt-dlp`
  - 유튜브 영상의 메타데이터와 미디어 처리 실험에 사용할 수 있는 오픈소스 도구다.
  - 영상 제목, 설명란, 길이, 채널명, 썸네일 등 레시피 추출에 필요한 기본 정보를 확인한다.
  - 플랫폼 약관과 저작권 이슈를 반드시 검토해야 한다.
  - 운영 환경에서는 무단 다운로드 구조가 문제가 될 수 있으므로, 링크 처리 범위와 저장 정책을 별도로 정해야 한다.

### 유튜브 메타데이터 활용

- 영상 제목
  - 요리 이름이 직접 들어 있을 가능성이 높다.
  - 제목에 여러 음식명이나 과장 문구가 섞일 수 있으므로 설명란, STT, OCR과 함께 검증한다.
- 영상 설명란
  - 재료, 계량, 조리 순서가 정리되어 있을 가능성이 높다.
  - 설명란에 명확한 재료와 계량이 있으면 STT나 OCR보다 우선 반영한다.
  - 설명란과 다른 입력이 충돌하면 설명란을 우선하되 충돌 내용을 `notes`에 남긴다.
- 채널명과 해시태그
  - 요리 영상 여부 판별과 카테고리 추정에 보조 정보로 사용한다.
  - 해시태그는 노이즈가 많을 수 있으므로 핵심 재료 판단에는 낮은 우선순위로 둔다.

### 음성 인식

- `Whisper`
  - 한국어 음성 인식에 사용할 수 있는 대표적인 오픈소스 STT 모델이다.
  - 영상에서 말로 설명되는 재료와 조리 과정을 추출하는 1차 수단으로 사용한다.
- `faster-whisper`
  - Whisper 계열 모델을 더 빠르게 실행하기 위한 후보로 검토한다.
  - 서버 비용과 응답 시간을 줄일 수 있는지 확인한다.
- `distil-whisper`
  - 응답 속도가 중요한 경우 후보로 검토한다.
  - 정확도와 처리 속도를 `faster-whisper`의 `large-v3` 계열과 비교한다.
- 보컬 분리 전처리
  - 유튜브 쇼츠는 배경음악이 큰 경우가 많아 STT 환각 또는 누락이 발생할 수 있다.
  - 필요한 경우 `Demucs`, `Spleeter` 같은 보컬 분리 도구를 사용해 음성 트랙을 먼저 분리한 뒤 STT를 수행한다.
  - 모든 영상에 적용하면 처리 시간이 늘어나므로, 음성 품질이 낮거나 STT 신뢰도가 낮은 경우에만 조건부 적용하는 방식을 검토한다.

### 화면 텍스트 추출

- OCR
  - 많은 요리 영상은 재료명과 계량을 음성이 아니라 화면 자막으로 제공한다.
  - 일정 간격으로 프레임을 캡처하고 OCR을 수행해 재료, 계량, 조리 팁을 추출한다.
  - VLM보다 비용과 속도 면에서 유리할 수 있으므로, VLM 기반 프레임 분석보다 먼저 검토한다.
  - 중복 자막과 장식 문구를 제거하기 위한 후처리 규칙이 필요하다.

### 텍스트 기반 레시피 추출

- Ollama 기반 로컬 LLM
  - 로컬 개발 환경에서 빠르게 실험하기 좋다.
  - 모델 후보는 한국어 성능, JSON 출력 안정성, 서버 자원 요구량을 기준으로 비교한다.
- 외부 AI API
  - 초기 품질 검증과 기준선 확보를 위해 검토할 수 있다.
  - 운영 비용, 개인정보, 장애 대응 정책을 함께 검토해야 한다.
  - 한국어 요리 텍스트 추출 기준선으로 비용 대비 성능이 좋은 경량 모델을 비교한다.
- 프롬프트 엔지니어링
  - 단순 추출 지시가 아니라 메타데이터, 영상 설명, STT, OCR 결과의 우선순위를 명확히 둔다.
  - 예를 들어 영상 설명란과 STT가 충돌하면 설명란을 우선하되, 충돌 내용을 `notes`에 남기는 식의 정책을 검토한다.
  - 요리 제목은 영상 제목과 설명란에서 먼저 추론하고, STT와 OCR은 보정 근거로 사용한다.
  - JSON 스키마를 강제하고, 확인되지 않은 계량은 추정값으로 표시하게 한다.

### 영상 프레임 분석

- Qwen-VL 계열
- LLaVA 계열
- Gemma vision 계열

프레임 분석은 영상에 재료가 보이지만 음성으로 설명되지 않는 경우를 보완하기 위한 2차 기능이다.

## 데이터 정규화 방향

AI가 추출한 재료명은 그대로 저장하지 않고 내부 표준 재료 체계와 연결하는 방식을 검토한다.

예시는 다음과 같다.

- `삼겹살`
- `돼지 삼겹살`
- `구이용 돼지고기`

위 표현들은 사용자의 의도에 따라 같은 재료로 볼 수 있으므로, 내부 DB의 표준 재료 ID와 매핑하는 Entity Linking 과정이 필요하다.

정규화가 필요한 이유는 다음과 같다.

- 재료 기반 검색 품질 개선
- 냉장고 재료 기반 추천 기능과 연동
- 같은 재료의 다른 표현을 하나로 묶는 통계 분석
- AI 추출 결과의 중복 제거

MVP에서는 표준 재료 사전을 작게 시작하고, 사용자의 수정 이력을 바탕으로 점진적으로 확장한다.

## 자체 모델 개발 전략

처음부터 모델을 직접 학습하는 방식은 비용과 데이터 요구량이 크다. 이 프로젝트에서는 다음 순서로 접근한다.

1. 오픈소스 모델 조합으로 기능을 만든다.
2. AI 추출 결과와 사용자가 수정한 최종 레시피를 함께 저장한다.
3. 실패 사례를 유형별로 분류한다.
4. 충분한 데이터가 쌓이면 프롬프트 개선, 후처리 규칙, 파인튜닝을 순서대로 검토한다.
5. 자체 모델은 특정 문제를 기존 모델 조합으로 해결하기 어렵다고 판단될 때 개발한다.

## 데이터 수집 방향

향후 자체 모델 개발 또는 파인튜닝을 위해 다음 데이터를 남기는 방안을 검토한다.

- 원본 영상 URL
- 영상 제목과 설명
- 채널명, 영상 길이, 해시태그 등 메타데이터
- STT 원문
- 추출된 재료 목록
- 추출된 조리 순서
- AI가 추정한 confidence 값
- 사용자가 수정한 최종 레시피
- 사용자가 삭제하거나 변경한 AI 추출 항목
- AI 초안과 사용자 최종본의 Diff 로그
- 낮은 confidence 항목에 대한 사용자 확인 여부

단, 원본 영상 파일과 플랫폼 콘텐츠를 서버에 저장할지 여부는 약관, 저작권, 저장 비용을 검토한 뒤 결정한다.

사용자 수정 이력은 모델 개선에 중요한 데이터다. 예를 들어 `설탕 1큰술`이 `설탕 2큰술`로 바뀐 경우와 `고추장`이 삭제되고 `고춧가루`가 추가된 경우는 오류 원인이 다를 수 있다. 따라서 최종본만 저장하기보다 어떤 항목이 어떻게 수정되었는지 구조화된 diff로 남기는 방식을 검토한다.

## 추출 결과 포맷 초안

```json
{
  "title": "김치볶음밥",
  "titleSource": "metadata_title",
  "sourceMetadata": {
    "url": "https://www.youtube.com/shorts/example",
    "videoTitle": "초간단 김치볶음밥 레시피",
    "descriptionUsed": true,
    "channelName": "example channel"
  },
  "ingredients": [
    {
      "name": "김치",
      "amount": "1컵",
      "confidence": 0.91,
      "source": "speech",
      "normalizedIngredientId": null,
      "needsReview": false
    },
    {
      "name": "밥",
      "amount": "1공기",
      "confidence": 0.88,
      "source": "speech",
      "normalizedIngredientId": null,
      "needsReview": false
    }
  ],
  "steps": [
    {
      "order": 1,
      "description": "김치를 잘게 썬다.",
      "confidence": 0.86
    },
    {
      "order": 2,
      "description": "팬에 기름을 두르고 김치를 볶는다.",
      "confidence": 0.82
    }
  ],
  "notes": [
    "정확한 계량은 영상에서 확인되지 않아 일부 추정됨."
  ],
  "sourcePriority": [
    "description",
    "metadata_title",
    "speech",
    "ocr",
    "vision"
  ]
}
```

## 백엔드 설계 방향

AI 추출은 일반 레시피 CRUD와 분리된 비동기 작업으로 설계한다.

예상 API 흐름은 다음과 같다.

```text
POST /api/v1/recipe-extractions
→ 링크 등록 및 작업 생성

GET /api/v1/recipe-extractions/{id}
→ 추출 상태와 결과 조회

POST /api/v1/recipes
→ 사용자가 수정한 최종 레시피 저장
```

작업 상태는 다음 값을 검토한다.

- `PENDING`
- `PROCESSING`
- `SUCCEEDED`
- `FAILED`
- `CANCELED`

비동기 작업 처리는 별도 큐 기반 구조를 검토한다.

- 영상 처리, STT, OCR, LLM 호출은 10초 이상 걸릴 수 있다.
- 백엔드 API 요청 스레드에서 직접 처리하지 않는다.
- Python 워커를 둘 경우 Redis와 Celery 조합을 검토한다.
- Kotlin/Spring 기반 워커로 유지할 경우 Redis Queue, RabbitMQ, Kafka, Spring Batch 또는 별도 Worker 프로세스를 비교한다.
- 사용자는 폴링, SSE, 푸시 알림 중 하나로 작업 완료를 전달받는다.
- MVP에서는 폴링으로 시작하고, 사용자 경험 개선 단계에서 SSE 또는 푸시 알림을 검토한다.

## 모바일 앱 설계 방향

모바일 앱에서는 AI 추출 기능을 레시피 작성 흐름의 보조 기능으로 제공한다.

- 레시피 작성 화면에서 영상 링크 입력 기능을 제공한다.
- 추출 중에는 작업 상태를 보여준다.
- 완료 후 제목, 재료, 조리 순서를 작성 폼에 채운다.
- AI 추정 결과임을 표시한다.
- confidence가 낮은 항목은 `확인이 필요해요` 같은 상태로 강조한다.
- 사용자가 수정하기 전에는 자동으로 공개 레시피로 등록하지 않는다.
- 공개 레시피로 전환할 때는 원본 영상 출처 링크를 표시하거나 확인하게 하는 UI를 검토한다.

## 정책 및 제약

- 영상 길이는 MVP에서 짧게 제한한다.
- 입력 링크는 유튜브 쇼츠 URL만 허용한다.
- 저작권이 있는 영상 파일을 장기 저장하지 않는다.
- 플랫폼 약관 위반 가능성이 있는 처리는 운영 전에 별도 검토한다.
- AI 추출 결과는 부정확할 수 있으므로 사용자 검토를 필수로 둔다.
- 알레르기, 식품 안전, 영양 정보는 확정 정보처럼 제공하지 않는다.
- 비공개 레시피 영상 링크와 사용자 수정 데이터는 개인정보 정책에 맞게 다룬다.
- 공개 레시피로 전환할 때 원저작자 출처 표시 정책을 검토한다.

## 단계별 작업 계획

### 1단계: 기능 정책 정리

- 지원할 링크 종류 결정
- 유튜브 쇼츠 URL 판별 기준 결정
- 영상 길이 제한 결정
- 원본 영상 저장 여부 결정
- AI 추출 결과의 사용자 고지 문구 결정
- 실패 시 사용자에게 보여줄 메시지 결정
- 존재하지 않는 영상, 쇼츠가 아닌 URL, 요리 레시피가 아닌 영상의 오류 응답 정책 결정
- 공개 레시피 전환 시 출처 표시 정책 결정
- 사용자 수정 데이터를 모델 개선에 사용할 때의 동의 방식 결정

### 2단계: 로컬 실험

- 샘플 유튜브 쇼츠 URL로 오디오 추출 실험
- 영상 제목과 설명란 메타데이터 추출 실험
- 제목과 설명란만으로 요리 제목, 재료, 조리 순서를 추출하는 기준선 실험
- Whisper 또는 faster-whisper로 한국어 STT 품질 확인
- `large-v3`, `distil-whisper` 계열의 속도와 정확도 비교
- 배경음악이 큰 영상에서 보컬 분리 전처리 효과 확인
- 프레임 OCR로 화면 자막과 재료 계량 추출 가능성 확인
- STT 결과만으로 레시피 JSON 생성 실험
- Ollama 기반 로컬 LLM 후보 비교
- 외부 경량 LLM API와 로컬 LLM의 비용, 속도, 한국어 품질 비교
- 요리 영상 여부를 먼저 판별해 불필요한 처리 비용을 줄일 수 있는지 확인
- 비레시피 영상과 존재하지 않는 URL 예외 처리 테스트

### 3단계: 백엔드 프로토타입

- AI 추출 작업 엔티티 설계
- AI 추출 결과 캐시 엔티티 설계
- 링크 등록 API 구현
- `video_id` 기준 기존 추출 결과 조회 흐름 구현
- 비동기 작업 처리 구조 검토
- STT 실행 방식 검토
- OCR 실행 방식 검토
- 추출 결과 JSON 저장 방식 결정
- 추출 결과 품질 상태 저장 방식 결정
- AI 초안과 사용자 최종본의 diff 로그 저장 방식 검토
- 표준 재료 ID 매핑 구조 검토

### 4단계: 모바일 연동

- 레시피 작성 화면에 영상 링크 입력 UI 추가
- 추출 상태 표시
- 추출 결과를 작성 폼에 반영
- 낮은 confidence 항목 강조 표시
- 사용자가 수정 후 저장하는 흐름 구현
- 공개 레시피 전환 시 원본 출처 표시 흐름 검토

### 5단계: 품질 개선

- 실패 사례 수집
- 프롬프트 개선
- 재료명 정규화
- 계량 단위 정규화
- OCR 중복 텍스트 제거
- STT, OCR, 메타데이터 간 충돌 처리 정책 개선
- 영상 제목과 설명란 기반 제목 추론 정확도 개선
- 사용자 수정 diff 기반 캐시 품질 상태 갱신
- 프레임 분석 추가 검토

### 6단계: 자체 모델 또는 파인튜닝 검토

- 사용자 수정 데이터를 학습 데이터로 사용할 수 있는지 정책 검토
- 데이터셋 스키마 정리
- 기존 모델 조합 대비 파인튜닝 효과 측정
- 자체 모델 운영 비용 산정

## 추출 결과 캐시 및 품질 관리 전략

AI 추출 결과는 정답이 아니라 사용자가 검토해야 하는 초안으로 저장한다. 같은 유튜브 쇼츠 링크가 다시 입력되더라도 캐시를 무조건 정답처럼 재사용하지 않는다.

### 기본 원칙

- AI 추출 결과는 공용 초안 캐시로 저장한다.
- 사용자가 수정해 저장한 레시피는 AI 캐시와 분리한다.
- 캐시 결과에는 품질 상태와 파이프라인 버전을 함께 저장한다.
- 잘못된 캐시는 재추출, 폐기, 관리자 검수 대상이 될 수 있어야 한다.

### 캐시 기준

캐시 조회의 기본 키는 전체 URL이 아니라 유튜브 `video_id`다.

예를 들어 다음 URL은 같은 영상으로 본다.

```text
https://youtube.com/shorts/zHMWRiJwrxA?si=abc
https://youtube.com/shorts/zHMWRiJwrxA?si=def
```

캐시 재사용 여부는 다음 값을 함께 고려한다.

- `platform`
- `video_id`
- `pipeline_version`
- `stt_model`
- `llm_model`
- `prompt_version`
- `description_hash`

프롬프트, 모델, 설명란이 바뀌면 같은 영상이라도 새로 추출할 수 있어야 한다.

### 품질 상태

캐시에는 다음 품질 상태를 둔다.

- `UNVERIFIED`
  - 최초 AI 추출 결과다.
  - 사용자 검토 전이므로 정답처럼 표시하지 않는다.
- `NEEDS_REVIEW`
  - 사용자 수정 폭이 크거나 검증 이슈가 있는 결과다.
  - 같은 영상 재요청 시 확인 필요 표시 또는 재추출을 검토한다.
- `VERIFIED`
  - 관리자 또는 충분한 사용자 피드백으로 검증된 결과다.
  - 같은 파이프라인 버전에서는 우선 재사용할 수 있다.
- `REJECTED`
  - 잘못된 결과로 판단된 캐시다.
  - 재사용하지 않고 재추출하거나 사용자에게 실패 상태를 안내한다.

### 재사용 정책

- `VERIFIED`
  - 기존 캐시를 바로 반환한다.
- `UNVERIFIED`
  - 기존 캐시를 반환하되 `AI 추정 결과`와 `확인 필요` 상태를 표시한다.
- `NEEDS_REVIEW`
  - 가능하면 재추출한다.
  - 재추출이 어렵다면 사용자에게 확인 필요 상태를 명확히 표시한다.
- `REJECTED`
  - 캐시를 사용하지 않는다.
  - 새 파이프라인 버전으로 재추출하거나 실패 상태를 반환한다.

관리자 또는 개발자용 API에는 강제 재추출 옵션을 둘 수 있다.

```json
{
  "url": "https://youtube.com/shorts/example",
  "force_refresh": true
}
```

이 옵션은 일반 사용자용 API에는 노출하지 않는다.

### 사용자 수정 피드백

사용자가 AI 초안을 수정해 저장하면 AI 초안과 최종본의 차이를 구조화된 diff로 저장한다.

활용 예시는 다음과 같다.

- `계란 1컵`이 반복적으로 `계란 1개`로 수정되면 계량 추출 오류로 본다.
- `고추장`이 삭제되고 `고춧가루`가 추가되면 STT 또는 LLM 추론 오류로 본다.
- 여러 사용자가 같은 항목을 비슷하게 수정하면 캐시 품질 상태를 `NEEDS_REVIEW`로 낮춘다.
- 수정 diff를 모아 프롬프트 개선, STT 보정, 표준 재료 매핑 개선에 사용한다.

### 데이터 분리

운영 단계에서는 다음 데이터를 분리한다.

- AI 추출 캐시
  - 영상 하나에 대한 공용 AI 초안이다.
  - `video_id`, 모델 버전, 프롬프트 버전, 품질 상태를 가진다.
- 사용자 레시피 초안
  - 특정 사용자가 AI 결과를 바탕으로 수정 중인 개인 초안이다.
  - AI 캐시와 사용자 수정본의 diff를 가진다.
- 최종 레시피
  - 사용자가 저장한 실제 레시피다.
  - 공개/비공개 정책과 사용자 소유권을 따른다.

### MVP 적용 범위

MVP에서는 다음까지만 구현한다.

- `video_id` 기준 AI 추출 결과 저장
- `pipeline_version`, `stt_model`, `llm_model`, `prompt_version` 저장
- 최초 저장 시 `quality_status=UNVERIFIED`
- 사용자에게 `AI 추정 결과` 표시
- 사용자 수정 후 최종 레시피 저장

2차 범위에서 다음을 추가한다.

- AI 초안과 사용자 최종본 diff 저장
- diff가 큰 캐시를 `NEEDS_REVIEW`로 변경
- 관리자/디버그용 강제 재추출 옵션
- `VERIFIED`, `REJECTED` 품질 상태 운영

## 우선순위

현재 프로젝트의 MVP 우선순위는 레시피 목록, 상세, 작성, 공개/비공개 흐름이다. AI 레시피 추출 기능은 핵심 레시피 CRUD가 안정화된 뒤 본격 구현한다.

다만 지금부터 다음 항목은 미리 고려한다.

- 레시피 생성 API가 AI 초안 입력을 받을 수 있는 구조
- 재료와 조리 순서를 구조화해서 저장할 수 있는 DB 모델
- 사용자가 AI 추출 결과를 수정한 이력을 나중에 학습 데이터로 활용할 수 있는 정책
- 같은 유튜브 쇼츠 재요청 시 캐시를 재사용하되 품질 상태와 버전을 확인하는 구조

## 미확정 사항

- 영상 플랫폼 약관 대응 방식
- 공개 레시피 전환 시 출처 표시 강제 여부
- 외부 AI API 사용 여부
- Ollama 운영 서버 도입 여부
- GPU 서버 필요 시점
- 사용자 수정 데이터를 학습에 사용할 때의 동의 방식
- OCR 도입 시점
- 보컬 분리 전처리 도입 시점
- 비동기 작업 큐 기술 선택
- 표준 재료 사전 구축 방식
- AI 추출 캐시 품질 상태 자동 갱신 기준
- 캐시 검증을 관리자 검수로 할지 사용자 피드백 집계로 할지 여부
