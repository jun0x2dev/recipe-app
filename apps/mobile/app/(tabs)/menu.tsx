import { MenuScreen } from '../../src/features/menu/screens/MenuScreen';

/**
 * - 메뉴 탭 라우트다.
 * - 좋아요한 레시피, 설정 등 부가 기능의 진입점이다.
 * - 하단 탭의 세 번째 주요 진입점이다.
 */
export default function MenuRoute() {
  return <MenuScreen />;
}
