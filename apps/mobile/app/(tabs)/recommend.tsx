import { RecommendScreen } from '../../src/features/recommend/screens/RecommendScreen';

/**
 * - 오메추(오늘의 메뉴 추천) 탭 라우트다.
 * - 하단 탭의 세 번째 진입점이다.
 */
export default function RecommendRoute() {
  return <RecommendScreen />;
}
