import { MenuScreen } from '../../src/features/menu/screens/MenuScreen';

/**
 * - 설정 탭 라우트다.
 * - 좋아요한 레시피, 계정 설정 등 부가 기능의 진입점이다.
 * - 하단 탭의 네 번째 진입점이다.
 */
export default function SettingsRoute() {
  return <MenuScreen />;
}
